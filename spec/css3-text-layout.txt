====== CSS Text Layout Level 3 ======

...

===== Current Issues =====

The following are resolved issues that will need spec text.

==== Issue 1 ====

  ;URL : 
  ;Summary : Need to define behavior of vertical block in horizontal flow.
  ;Resolution : "max-width: none" behaves as "max-width: 100vh". Analogously for horizontal blocks in vertical flow.
  ;Status : Needs edits, either here or in CSS3 Box.

==== Issue 2 ====

  ;URL : http://lists.w3.org/Archives/Member/w3c-css-wg/2007JulSep/0177.html
  ;Summary : Need to define shift direction for vertical text
  ;Resolution : Shift direction (direction of superscript wrt baseline) by default is to the right of the baseline in both lr and rl block progressions.
  ;Status : Needs edits
